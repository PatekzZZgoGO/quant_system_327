# commands

当前命令仍处于过渡状态。
当前命令文件尚未迁移，现有 `data.py`、`factor.py`、`ic.py`、`backtest.py` 仍保持在原位置。
后续会按 `shared/`、`product/`、`trading/` 三个分组逐步整理命令入口。
在完成迁移前，这里仍然是现有命令的直接存放位置。
后续新增命令应优先落到对应分组目录，而不是继续堆在根下。
当前根下命令文件应尽量保持为薄兼容入口，只负责参数注册、调用 pipeline 与结果输出，不继续承载业务编排、批量调度或底层数据访问。
