# infra

Classification: `Shared Foundation`

当前目录覆盖 `config/`、`logging/`、`monitoring/` 以及 `database/` 等基础设施能力。
其中 `config/logging/monitoring` 已经具备明显的共享地基属性，是当前主干优先维护的部分。
`database/` 仍更依赖具体运行链路，因此目录整体共享时需要区分内部子边界。
